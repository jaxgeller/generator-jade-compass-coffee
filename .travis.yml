language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-webapp' ]; then cd .. && eval "mv $currentfolder generator-webapp" && cd generator-webapp; fi

